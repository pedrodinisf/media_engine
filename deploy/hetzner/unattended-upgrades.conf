// media_engine — unattended-upgrades policy for the Hetzner deploy.
//
// Installed to /etc/apt/apt.conf.d/52unattended-upgrades-local by
// bootstrap.sh. Sits on top of the distribution defaults and tightens
// them: SECURITY pocket only (no surprise feature updates), no
// auto-reboot (solo operator verifies reboots manually).

Unattended-Upgrade::Allowed-Origins {
    "${distro_id}:${distro_codename}-security";
    "${distro_id}ESMApps:${distro_codename}-apps-security";
    "${distro_id}ESM:${distro_codename}-infra-security";
};

Unattended-Upgrade::AutoFixInterruptedDpkg "true";
Unattended-Upgrade::MinimalSteps "true";
Unattended-Upgrade::Remove-Unused-Kernel-Packages "true";
Unattended-Upgrade::Remove-Unused-Dependencies "true";

// No automatic reboot. New kernels land on disk but the operator picks
// the maintenance window.
Unattended-Upgrade::Automatic-Reboot "false";

// Activate the periodic timer.
APT::Periodic::Update-Package-Lists "1";
APT::Periodic::Download-Upgradeable-Packages "1";
APT::Periodic::AutocleanInterval "7";
APT::Periodic::Unattended-Upgrade "1";
